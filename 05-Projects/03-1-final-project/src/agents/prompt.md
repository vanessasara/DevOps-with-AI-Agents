# jira agent prompt 

uv run jira_agent.py "Create a Task issue in project KAN with summary: set up ArgoCD pipeline, description: configure ArgoCD for GitOps deployment pipeline on k3s cluster, priority: Medium"

# github agent prompt

uv run github_agent_with_mcp.py "what was the last commit in my vanessasara/Devops-practice-repo"
